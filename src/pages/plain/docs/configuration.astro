---
// Auto-generated by scripts/regen-plain.mjs. Edit the marketing source and re-run.
// plain-source: src/pages/docs/configuration.astro
// plain-source-sha256: 534c864f2831d5f2f6b2fe43d81dce7d0dc919a8e58ddc8c0395ba741a685adf
import PlainLayout from '../../../layouts/PlainLayout.astro';
---
<PlainLayout title="Configuration — Pilot Protocol (plain)" description="Config files, environment variables, directory structure, and daemon flags for Pilot Protocol." canonical="https://pilotprotocol.network/plain/docs/configuration/">

<p><a href="/plain/docs/">&larr; Docs index</a></p>

<h1>Configuration</h1>

<p>This document describes configuration files, environment variables, directory structure, and daemon flags.</p>

<h2>Config file</h2>
<p>Configuration is stored in ~/.pilot/config.json.</p>
<pre><code>&#123;
  "registry": "34.71.57.205:9000",
  "beacon": "34.71.57.205:9001",
  "hostname": "my-agent",
  "email": "user@example.com",
  "socket": "/tmp/pilot.sock",
  "webhook": "http://localhost:8080/events"
&#125;</code></pre>
<p>CLI flags have the highest precedence. For pilotctl daemon start, the config file is consulted before environment variables. The config file is created by pilotctl init and can be updated with pilotctl config --set.</p>

<h2>Config commands</h2>
<p>Initialize the configuration file:</p>
<pre><code>pilotctl init --hostname my-agent</code></pre>
<p>This creates ~/.pilot/config.json with the specified settings.</p>
<p>View the current configuration:</p>
<pre><code>pilotctl config</code></pre>
<p>Set a configuration value:</p>
<pre><code>pilotctl config --set registry=host:9000
pilotctl config --set hostname=new-name</code></pre>

<h2>Automatic updates</h2>
<p>A pilot-updater sidecar can keep binaries on the latest stable release. Automatic updates are off by default.</p>
<p>Check the update status:</p>
<pre><code>pilotctl update status</code></pre>
<p>This shows whether automatic updates are enabled and the current version.</p>
<p>Enable or disable automatic updates:</p>
<pre><code>pilotctl update enable
pilotctl update disable</code></pre>
<p>The setting is stored in ~/.pilot/auto-update.json and takes effect on the updater's next check. When enabled, the updater installs new stable releases after verifying release checksums.</p>
<p>Run a single manual update:</p>
<pre><code>pilotctl update</code></pre>
<p>This runs a single check-and-install, regardless of the automatic-update setting. Pin to a specific release with --pin &lt;version&gt;.</p>

<h2>Environment variables</h2>
<ul>
  <li>PILOT_SOCKET: Path to the daemon IPC socket. Default: /tmp/pilot.sock</li>
  <li>PILOT_REGISTRY: Registry server address. Default: 34.71.57.205:9000</li>
  <li>PILOT_BEACON: Beacon server address for STUN, NAT punch, and relay. Default: 34.71.57.205:9001</li>
  <li>PILOT_DAEMON_BIN: Override path to the pilot-daemon binary. Default: auto-discovered</li>
  <li>PILOT_GATEWAY_BIN: Override path to the pilot-gateway binary. Default: auto-discovered</li>
  <li>PILOT_HOSTNAME: Hostname to set during install. If unset, the node is assigned an internal hostname of the form pilot-XXXXXXXX. Default: none</li>
  <li>PILOT_ADMIN_TOKEN: Admin token for enterprise operations. Default: none</li>
  <li>PILOT_HOME: Directory where pilot stores identity, config, and received files. Default: ~/.pilot</li>
  <li>PILOT_EMAIL: Email address used for first-time daemon registration. Default: none</li>
  <li>PILOT_RC: Set to 1 to install from the edge (release-candidate) channel. This is a legacy alias for --channel edge. Default: unset</li>
</ul>

<h2>Directory structure</h2>
<pre><code>~/.pilot/
  bin/
  bin/.pilot-version
  config.json
  identity.json
  trust.json
  setups/
  received/
  inbox/
  pilot.pid
  pilot.log
  updater.log</code></pre>
<ul>
  <li>bin/: Installed binaries (pilot-daemon, pilotctl, pilot-updater; pilot-gateway if installed separately)</li>
  <li>bin/.pilot-version: Current version (used by auto-updater)</li>
  <li>config.json: Configuration file</li>
  <li>identity.json: Ed25519 keypair (persistent identity)</li>
  <li>trust.json: Trust state (trusted peers, pending requests)</li>
  <li>setups/: Setup manifests (role identity files)</li>
  <li>received/: Files received via data exchange</li>
  <li>inbox/: Messages received via data exchange</li>
  <li>pilot.pid: Daemon PID file</li>
  <li>pilot.log: Daemon log file</li>
  <li>updater.log: Auto-updater log file</li>
</ul>

<h2>Daemon flags</h2>
<p>These flags are forwarded from pilotctl daemon start to the underlying pilot-daemon binary.</p>
<ul>
  <li>--registry &lt;addr&gt;: Registry server address</li>
  <li>--beacon &lt;addr&gt;: Beacon server address (STUN)</li>
  <li>--listen &lt;addr&gt;: Local UDP listen address (default: :0)</li>
  <li>--endpoint &lt;addr&gt;: Fixed public endpoint for cloud VMs (skips STUN)</li>
  <li>--identity &lt;path&gt;: Path to identity key file</li>
  <li>--email &lt;addr&gt;: Email address for account identification and key recovery</li>
  <li>--hostname &lt;name&gt;: Register with this hostname</li>
  <li>--public: Start as a public node</li>
  <li>--no-encrypt: Disable tunnel encryption</li>
  <li>--foreground: Run in the current process (don't fork)</li>
  <li>--trust-auto-approve: Auto-approve every incoming trust handshake</li>
  <li>--log-level &lt;level&gt;: Log level: debug, info, warn, error</li>
  <li>--log-format &lt;fmt&gt;: Log format: text or json</li>
  <li>--socket &lt;path&gt;: IPC socket path (default /tmp/pilot.sock)</li>
  <li>--config &lt;path&gt;: Config file path</li>
  <li>--webhook &lt;url&gt;: Webhook URL for event notifications</li>
  <li>--admin-token &lt;token&gt;: Admin token for network create/admin operations</li>
  <li>--networks &lt;ids&gt;: Comma-separated network IDs to auto-join on startup</li>
  <li>--wait &lt;dur&gt;: How long to wait for the daemon to become ready (default 15s)</li>
</ul>
<p>The following tuning flags are only consumed when invoking the pilot-daemon binary directly. They are not forwarded by pilotctl daemon start.</p>
<ul>
  <li>-keepalive &lt;dur&gt;: Keepalive probe interval. Default: 60s</li>
  <li>-idle-timeout &lt;dur&gt;: Idle connection timeout. Default: 120s</li>
  <li>-syn-rate-limit &lt;n&gt;: Max SYN packets per second. Default: 100</li>
  <li>-max-conns-per-port &lt;n&gt;: Max connections per port. Default: 1024</li>
  <li>-max-conns-total &lt;n&gt;: Max total connections. Default: 65536</li>
  <li>-time-wait &lt;dur&gt;: TIME_WAIT duration. Default: 10s</li>
  <li>-registry-tls: Enable TLS for registry connection. Default: false</li>
  <li>-registry-fingerprint &lt;hex&gt;: SHA-256 fingerprint of registry TLS cert. Default: none</li>
  <li>-registry-trust &lt;mode&gt;: Trust store for -registry-tls: pinned or system. Default: pinned</li>
  <li>-no-echo: Disable the built-in echo service (port 7). Default: false</li>
  <li>-no-dataexchange: Disable the built-in data exchange (port 1001). Default: false</li>
  <li>-dataexchange-b64: Include a lossless data_b64 field in inbox messages for binary payloads. Default: false</li>
  <li>-no-eventstream: Disable the built-in event stream (port 1002). Default: false</li>
  <li>-relay-only: Hide real_addr from peers; reach only via beacon-relay. Default: false</li>
  <li>-transport &lt;mode&gt;: Tunnel transport: udp (default) or compat (WSS). Default: udp</li>
  <li>-compat-beacon &lt;url&gt;: WSS endpoint used when -transport=compat. Default: wss://beacon.pilotprotocol.network/v1/compat</li>
  <li>-tls-trust &lt;mode&gt;: Compat-mode TLS trust store: system (default) or pinned. Default: system</li>
  <li>-advertise-endpoint &lt;host:port&gt;: Override the STUN-discovered endpoint advertised to the registry. Default: none</li>
</ul>

<h2>Logging</h2>
<p>The daemon uses structured logging and writes to ~/.pilot/pilot.log.</p>
<pre><code># Debug logging
pilotctl daemon start --log-level debug

# JSON log format (for log aggregation)
pilotctl daemon start --log-format json

# View logs
tail -f ~/.pilot/pilot.log</code></pre>
<p>Log levels are debug, info (default), warn, and error.</p>

<h2>Setup manifests</h2>
<p>When a setup skill configures an agent for a specific role, it writes a setup manifest to ~/.pilot/setups/&lt;slug&gt;.json. This file persists the role configuration.</p>
<pre><code>&#123;
  "setup": "fleet-health-monitor",
  "setup_name": "Fleet Health Monitor",
  "role": "web-monitor",
  "role_name": "Web Server Monitor",
  "hostname": "acme-web-monitor",
  "description": "Watches nginx/app health, CPU, memory, response times.",
  "skills": &#123;
    "pilot-health": "Check nginx, app endpoints, SSL certs.",
    "pilot-alert": "Publish alerts to acme-alert-hub on health-alert.",
    "pilot-metrics": "Collect CPU, memory, disk, response time."
  &#125;,
  "peers": [
    &#123;
      "role": "alert-hub",
      "hostname": "acme-alert-hub",
      "description": "Central alert aggregator"
    &#125;
  ],
  "data_flows": [
    &#123;
      "direction": "send",
      "peer": "acme-alert-hub",
      "port": 1002,
      "topic": "health-alert",
      "description": "Health check failures"
    &#125;
  ],
  "handshakes_needed": ["acme-alert-hub"]
&#125;</code></pre>
<p>Manifest fields:</p>
<ul>
  <li>setup (string): Setup slug (matches the filename)</li>
  <li>setup_name (string): Human-readable setup name</li>
  <li>role (string): This agent's role ID within the setup</li>
  <li>role_name (string): Human-readable role name</li>
  <li>hostname (string): This agent's hostname</li>
  <li>description (string): What this agent does in context</li>
  <li>skills (map): Skill name to a contextual description</li>
  <li>peers (array): All peer agents in the setup</li>
  <li>data_flows (array): Directional data flows (send/receive) with port and topic</li>
  <li>handshakes_needed (array): Hostnames of peers that need direct trust</li>
</ul>
<p>The manifest is a convention used by the AI agent. No daemon or CLI changes are required.</p>

</PlainLayout>
